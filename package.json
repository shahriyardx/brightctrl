{
  "name": "brightctrl",
  "version": "0.0.5",
  "description": "A lightweight DDC/CI monitor brightness controller for Linux",
  "type": "module",
  "bin": {
    "brightctrl": "dist/index.js",
    "bctrl": "dist/index.js",
    "bc": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "bun --watch src/index.tsx",
    "build": "bun build src/index.tsx --outdir dist --target node --format esm --external ink --external react --external react-reconciler --external scheduler --external '@types/react'",
    "prepublishOnly": "bun run build",
    "lint": "biome check src/",
    "format": "biome check --write src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ink": "^7.0.3",
    "react": "^19.2.6",
    "smol-toml": "^1.6.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/node": "^25.8.0",
    "@types/react": "^19.2.14",
    "typescript": "^6.0.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shahriyardx/brightctrl.git"
  },
  "homepage": "https://github.com/shahriyardx/brightctrl",
  "keywords": [
    "brightness",
    "ddc-ci",
    "ddcutil",
    "monitor",
    "tui",
    "ink",
    "waybar"
  ],
  "license": "MIT"
}
